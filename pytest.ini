[pytest]
minversion = 6.0
addopts = -ra -q --doctest-glob='*.md'
testpaths = tests
markers =
    integration: Tests requiring internet access
    local: tests that are not suitable for CI
    usechrome: test uses Chrome browser
    usebrowser: test uses browser
