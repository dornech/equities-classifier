[pytest]
minversion = 6.0
addopts = -ra -q --doctest-glob='*.md'
testpaths = tests
markers =
    integration: integration test cases
    local: test cases not suitable for CI
    usechrome: test cases use/require Chrome browser
    usebrowser: tests cases use/require browser
